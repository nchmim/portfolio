/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'export',
  basePath: '/nchmim',
  experimental: {
    appDir: true,
  },
}

module.exports = nextConfig
